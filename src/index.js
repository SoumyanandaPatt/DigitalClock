import ReactDOM from 'react-dom';
import React from 'react';
import App from './App';
//Rendering something into the browser
ReactDOM.render(

        <App/>,
        document.getElementById('root')
);